@inherits AccionesTareaComponentaBase
<MudCard Class="py-2 mb-2">
    <MudCardContent>
        <MudButton @onclick="OpenAddTareaDialog" Disabled="@(MetaId > 0 ? false : true )" Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Add" Color="Color.Default">Agregar</MudButton>
        <MudButton @onclick="CompleTarTareas" Variant="Variant.Filled" Disabled="@(Tareas.Count > 0 ? false : true )" StartIcon="@Icons.Material.Filled.CheckCircle" Color="Color.Success">Completar tarea</MudButton>
        <MudButton @onclick="OpenUpdateTareaDialog" Disabled="@(TareaId > 0 ? false : true )" Variant="Variant.Filled" StartIcon="@Icons.Material.Filled.Edit" Color="Color.Warning">Editar</MudButton>
        <MudButton @onclick="OpenDeleteTareasDialog" Variant="Variant.Filled" Disabled="@(Tareas.Count > 0 ? false : true )" StartIcon="@Icons.Material.Filled.Delete" Color="Color.Error">Eliminar</MudButton>
    </MudCardContent>
</MudCard>